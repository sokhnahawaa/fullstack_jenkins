apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend       # Nom du déploiement frontend
  labels:
    app: frontend
spec:
  replicas: 1          # Nombre de pods frontend
  selector:
    matchLabels:
      app: frontend    # Label utilisé pour associer le pod au déploiement
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: soxnahawaa/jenkins_front:latest   # Image Docker du frontend
        ports:
        - containerPort: 80    # Port exposé par le conteneur (nginx ou serveur HTTP)
        env:
        - name: VITE_API_URL  
          value: "http://172.17.0.2:30001/api"  # URL du backend via NodePort
